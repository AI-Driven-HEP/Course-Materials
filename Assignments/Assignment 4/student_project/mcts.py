
from .tree import Node

def mcts(root, net, n_simulations=50):
    """
    👉 Students: Replace this with your own model!
    - Input: root (9 board cells --> Node(env.clone()) ) , net (instance of Model(nn.Module))
    - Output: move --> int number between 0 to 8
    """
    for _ in range(n_simulations):
        pass
