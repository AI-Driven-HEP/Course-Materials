# pyproject.toml
[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "student_project"
version = "0.1.0"
requires-python = ">=3.9"

[tool.setuptools]
packages = ["student_project"]   # root layout
